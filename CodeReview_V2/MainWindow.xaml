<Window x:Class="CodeReview_V2.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MainWindow" Height="350" Width="525">
    <Window.Resources>
        <ResourceDictionary Source="MainWindowResources.xaml" />
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="90*"/>
            <RowDefinition Height="10"/>
        </Grid.RowDefinitions>
        <DataGrid Grid.Row="0" AutoGenerateColumns="False" 
                  Name="IncidentAssociations" HorizontalAlignment="Left" VerticalAlignment="Top" 
                  CanUserAddRows="False" IsReadOnly="True" SelectionMode="Extended" Width="500">
            <DataGridTextColumn Binding="{Binding Path=ChangeSets.Files.Filename}" Header="Filename" Width="40*"/>
            <DataGridTextColumn Binding="{Binding Path=ChangeSets.Author}" Header="Author" Width="10*"/>
        </DataGrid>
        <Button Content="Test" Click="Button_Click"/>
    </Grid>
</Window>
